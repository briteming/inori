---
const { date } = Astro.props;

const formatDate = typeof date === 'string' ? new Date(date) : date;
---

<time datetime={formatDate.toISOString()}>
	{
		formatDate.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
